# magic-workshop — ワークショップ用 AIアプリ生成コンソール 要件定義

## 概要

小中高生向けプログラミングワークショップで先生が使う、ブラウザベースの操作コンソール。
最大4チーム分のペインを表示し、先生が自然言語プロンプトを打つとAIが単一HTMLの
ミニアプリ(ゲーム・クイズ・学習ツール等)を生成する。生成物はチームごとの固定URLで
配信され、子どもたちはQRコードから自分のスマホでアクセスする。

新しいバージョンができると、子どものスマホ画面上部に**魔法の杖ボタン**がキラキラした
演出付きで出現し、タップすると最新版に切り替わる(「自分で魔法をかける」体験)。

先生とAIのやり取りはコンソール下部に全チーム分が流れ、プロジェクターで
投影することで他チームのプロンプトの工夫が全員の参考になる。

## システム構成

- 単一のNode.jsサーバー(Express等)。DBなし、状態はすべてファイルシステム。
- 先生用コンソール: SPA(ビルド不要。プレーンJS + ESモジュールで可)
- 生徒用配信: 静的ファイル + シェルページ
- サーバー→ブラウザの更新通知は SSE(片方向で足りるためWebSocket不要)

## ディレクトリ構造(サーバー側の状態)

```
data/
  team1/
    meta.json          # チーム名、絵文字、現在のバージョン番号
    history.json       # やり取りログ [{v, prompt, reply, timestamp, status}]
    v1.html v2.html …  # 生成物(単一HTML、世代保存。UIには出さない保険)
    current.html       # 配信中バージョンへのコピー(アトミック差し替え)
  team2/ …
prompt-template.md     # 固定システムプロンプト(コードと分離、リハーサルで調整する)
config.json            # 生成バックエンド設定(claude | codex)ほか
```

## URL設計

- `/console` … 先生用コンソール
- `/t/:teamId/` … 生徒用シェルページ(QRコードはこのURLを指す)
- `/t/:teamId/app?v=N` … 生成HTML本体(iframe内に表示)
- `/t/:teamId/events` … 生徒シェル向けSSE(新バージョン通知)
- `/api/teams/:id/generate` … POST {prompt, mode: "edit"|"new"}
- `/api/state` … GET 全チームのmeta+history(コンソール初期化用)
- `/api/events` … コンソール向けSSE(全チームの状態変化・ログ追記)
- `/api/teams/:id/publish` … 現在版を変更不可の作品スナップショットとして保存
- `/published/:publicationId/` … 開発時の保存作品配信。本番は作品専用ドメインから配信

## 生徒用シェルページ

- 全画面iframeで `/t/:teamId/app?v=現在版` を表示
- 初回生成前は「じゅんびちゅう…」のプレースホルダを表示
- SSE(切断時は5秒ポーリングにフォールバック)で新バージョンを検知
- **新バージョン検知時: 画面上部に魔法の杖ボタンが出現する。**
  - キラキラ・光彩などのマジカルな演出(CSSアニメーション+パーティクル)
  - タップすると光のエフェクトとともにiframeが最新版へ切り替わり、ボタンは消える
  - 未タップのまま次のバージョンが出た場合も、タップで常に最新版へ飛ぶ
  - 遊びを妨げない: ボタンが出るだけで、勝手に画面は切り替えない
- シェル自体のUIは最小限。iframe中のアプリが主役

## 先生用コンソール

### レイアウト

- **上部: 4ペインを横一列に並べる。** 各ペインの構成:
  - チーム名+絵文字(クリックで編集)
  - QRコード(常時表示、`/t/:teamId/` を指す。クリックで拡大モーダル)
  - HTMLプレビュー(sandbox付きiframe、配信中バージョンと同一)
  - 生成中はペイン内に「考え中…」のささやかな演出
    (Codex/Claude風のシマー・スピナー程度。コードの中身は見せない)
  - 「作品保存」ボタン。保存時点のHTMLを固定URL化し、共有用QRを表示する
- **下部: 全チーム共有のやり取りエリア。**
  - スクロールするログ: 先生のプロンプトとAIの返答が時系列で流れる
  - 各エントリはどのチーム宛かをチーム色+絵文字で明示
  - AIの返答は「何を作った/直したか」の子ども向け一文(後述)
  - 生成中のエントリは「考え中…」表示
  - 最下部にプロンプト入力欄(複数行可、Cmd/Ctrl+Enterで送信)
- **アクティブペイン**: プロンプトの宛先となるペインは枠のハイライト等で
  明確に区別する。入力欄の近くにも宛先チーム名を表示

### キーボードショートカット

- `1`〜`4`: アクティブペイン(プロンプトの宛先)を切り替え
- `Esc`: 入力欄からフォーカスを外す/モーダルを閉じる
- 入力欄フォーカス中は数字キーを奪わない(切り替えは入力欄外でのみ)
- あるチームが生成中でも、別チームに切り替えてプロンプト送信できる

## 生成フロー

1. 先生がプロンプト送信 → history.jsonにstatus:"generating"で追記、
   ログとペインに「考え中」を即時反映(SSE)
2. サーバーは生成バックエンド(後述)を起動しHTMLを生成
3. 完了したらHTMLと返答文を抽出・検証、vN.htmlとして保存、
   current.htmlをアトミックに差し替え、バージョン番号をインクリメント
4. 生徒シェルへ新バージョン通知(魔法ボタン出現)、
   コンソールへログ確定+プレビュー更新を通知
- **editモード(既定)**: 直近の生成HTML全文+今回のプロンプトを渡す
- **newモード**: テンプレート+今回のプロンプトのみ(「最初から作り直す」トグル)
- 同一チームの多重生成は禁止(そのチームが生成中は宛先にした送信をロック)。
  別チームの並行生成は許可(最大4並行)
- タイムアウト(180秒)や生成失敗時はhistoryにstatus:"failed"を記録し、
  配信中バージョンは変更しない。ログにエラーと「再試行」を表示

## 生成バックエンド(CLIエージェント抽象化)

- インターフェースは1関数に閉じ込める:
  `generate({systemTemplate, mode, baseHtml, prompt}) → {html, reply}`
- 実装はローカルCLIのサブプロセス実行。**APIキー・従量課金は使わない**
  (各CLIのサブスクリプション内で動かす):
  - `claude`(既定): `claude -p <prompt> --output-format text` 相当で単発実行
  - `codex`: `codex exec <prompt>` 相当で単発実行
  - 先生用コンソールから注文ごとに切り替え。config.json(または環境変数
    `GENERATOR=claude|codex`)は画面の初期選択を決める。
    新バックエンドを足しやすいよう、バックエンドごとに1モジュールに分離
- 出力の規約: バックエンドへの指示で
  「```html フェンスで完全なHTMLを出力し、その後に子ども向けの一文
  (何を作った/変えたか)を書く」ことを義務付け、サーバー側で
  フェンス内HTMLと返答文をパースする。抽出失敗はエラー扱い
- CLIが対話モードに入らないよう非対話フラグ・権限スキップ等を適切に付与する

## prompt-template.md に入れる品質要件(初期値)

- 出力は完全な単一HTMLファイルのみ(CSS/JSインライン)
- 外部リソース(CDN・画像URL・Webフォント)禁止
- スマートフォン縦画面前提: viewport meta必須、タッチ操作、
  ボタンは大きく、ホバー依存禁止
- UIテキストは日本語(小中学生が読める表現)
- データ保存が必要ならlocalStorage、効果音はWeb Audio APIで合成
- 対象は小中高生。明るくポジティブな見た目
- HTMLの後に、作った/変えた内容を子ども向けに一文で説明する

## 非機能要件

- 生徒側はHTTPSで配信できること(モバイル回線アクセスで警告を出さない。
  リバースプロキシ配下での動作を想定し、絶対URL生成に注意)
- サーバー再起動後も data/ から全状態(履歴・バージョン)を復元できること
- コンソールは1920×1080のプロジェクター投影で後方の席からも読める文字サイズ
- コンソールは簡易トークン(URLクエリまたはBasic認証)で保護。生徒用URLは無認証
- チーム数は定数 `TEAM_COUNT = 4` とし、コード上ハードコードを散らさない
- 保存作品は生成コンソールと別オリジンで配信し、検索エンジンへは `noindex` を返す
- 保存作品IDは日付・チーム・版にランダム値を加え、URLの総当たりを困難にする
- 保存先は再起動で消えない永続ボリュームとし、ホスト交換に備えてバックアップ対象に含める

## 非目標(作らないもの)

- 生徒側のアカウント・入力機能
- 履歴からのロールバックUI(世代ファイルの保存のみ。復旧は手動で可)
- 生成中コードのストリーミング表示・プログレスバー
- チーム間ランキング等のサーバーサイドゲームロジック
- DB、ビルドパイプライン、テストの網羅
